# Frank-Gateway Helm chart

The Frank gateway is based on APISIX, it will turn the APISIX gateway in a two way gateway and add specific plugins for this and other usecases. 

We run in standalone mode only, for this usecase we created a userinterface managing the apisix.yml and making sure it is correct before uploading it.

